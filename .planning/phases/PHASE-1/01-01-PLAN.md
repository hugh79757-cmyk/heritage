# PLAN.md - Phase 1: UI Fixes and Improvements

## Objective
Fix the quarter-circle button appearance on palace cards and apply previously recommended UI/UX improvements.

## Tasks

### 1. Investigate and Fix Card Button Issue
- **Location**: `src/components/DancheongCard.astro`
- **Problem**: The button (the whole `<a>` tag) appears as a quarter-circle shape, likely due to border-radius or clip-path from global styles.
- **Action**:
  - Inspect computed styles for the card link.
  - Identify any conflicting CSS (e.g., from `.card-base` or global radius utilities).
  - Adjust the card's border-radius to a consistent value (e.g., `var(--radius-lg)`) and ensure no clipping.
  - If the shape is intentional but looks wrong, consider changing the design to a standard rounded rectangle with appropriate padding.

### 2. Refine Card Layout and Spacing
- Ensure each card has consistent internal padding using the 8px base spacing system (`var(--space-6)` etc.).
- Verify that the card image, title (Hanja), name, and description have proper vertical rhythm.
- Make sure the hover effect (lift and shadow) is smooth and does not cause layout shifts.

### 3. Apply Pastel Color Scheme & Ma (Whitespace) Principles
- Confirm that all components use the pastel variables defined in `global.css`:
  - `--dancheong-blue-pastel`, `--dancheong-red-pastel`, `--dancheong-yellow-pastel`, `--dancheong-white-pastel`, `--dancheong-black-pastel`.
- Ensure generous whitespace (margins/padding) follows the 8px grid, creating a breathable layout.
- Check that text contrast meets WCAG AA on pastel backgrounds.

### 4. Verify Responsiveness
- Test the palace cards grid at breakpoints:
  - Mobile (<640px): single column.
  - Tablet (>=640px): two‑column layout (as currently defined).
  - Desktop (>=768px): maintain current bento‑grid proportions.
- Ensure the hero gate animation and particle effects respect `prefers-reduced-motion`.

### 5. Update SEO & Metadata
- Add JSON‑LD structured data for each palace (type `TouristAttraction` or `HistoricalPlace`).
- Ensure Open Graph and Twitter cards reflect the correct language variant.

### 6. Build, Test, and Deploy
- Run `npm run build` to verify no errors.
- Preview locally (`npm run preview`) and manually test.
- Deploy to Cloudflare Workers via `wrangler deploy`.

## Verification Criteria
- [ ] Palace cards display as uniform rounded rectangles, not quarter‑circles.
- [ ] Card hover lift works without clipping or jitter.
- [ ] Spacing follows the 8px baseline grid throughout the section.
- [ ] Pastel colors are used consistently; no fallback to neon colors unless intentional.
- [ ] Page loads successfully in all supported languages (ko, en, ja, zh).
- [ ] Lighthouse performance score ≥ 90 (mobile) after changes.
- [ ] No console errors or warnings.

## Notes
- Reference the prior feedback on pastel dancheong palette and ma spacing.
- Keep the existing gate‑opening hero animation and particle effects intact.
- Do not remove the Google Translate banner; it remains as a fallback.

---
*Prepared by Sisyphus (Orchestrator) – 2026‑07‑12*