# 프로젝트 분석 보고서

## 1. 프로젝트 구조

### 디렉토리 트리 (깊이 3)
```
.
├── pipeline/
│   ├── config/
│   │   └── buildings.json
│   ├── scripts/
│   ├── optimized/
│   │   ├── high/
│   │   ├── low/
│   │   ├── posters/
│   │   └── mid/
│   ├── converted/
│   │   ├── high/
│   │   ├── low/
│   │   └── mid/
│   └── raw/
│       ├── datagokr/
│       └── sketchfab/
├── .astro/
│   ├── collections/
│   ├── content.d.ts
│   ├── data-store.json
│   ├── settings.json
│   └── types.d.ts
├── dist/
│   ├── _worker.js/
│   │   ├── chunks/
│   │   │   └── astro/
│   │   └── pages/
│   │       └── palace/
│   │           └── _id_/
│   ├── _astro/
│   └── _routes.json
├── node_modules/
├── public/
├── scripts/
├── src/
│   ├── lib/
│   │   ├── api.ts
│   │   └── landmarks.ts
│   └── pages/
│       └── sitemap.xml.ts
└── .vscode/
    ├── extensions.json
    └── launch.json
```

### 파일별 줄 수 (소스 파일만)
- `backup3.py`: 610줄
- `backup2.py`: 349줄
- `src/lib/api.ts`: 216줄
- `src/lib/landmarks.ts`: 209줄
- `.astro/content.d.ts`: 199줄
- `src/pages/sitemap.xml.ts`: 40줄
- `.astro/types.d.ts`: 1줄

**총 소스 파일: 7개, 총 줄 수: 1,624줄**

### 진입점 식별
- 메인 애플리케이션: `astro.config.mjs` (Astro 기반 웹사이트)
- 진입 스크립트: `backup3.py`, `backup2.py` (데이터 처리 백업 스크립트)
- 웹 진입점: `src/pages/` (Astro 페이지)
- API 엔드포인트: `src/lib/api.ts` (외부 API 연동)

## 2. 코드 품질 스캔

### 50줄 초과 함수 목록

#### Python 파일 (`backup3.py`)
1. `extract_signatures_python`: 54줄 (lines 211-264)
   - 1) Python 코드에서 함수/클래스 시그니처 추출 → **분리 권장**
   - 2) AST 파싱 및 분석
   - 3) 시그니처 문자열 생성

2. `export_project`: 146줄 (lines 439-584) ⚠️ **긴 함수**
   - 1) 파일 시스템 스캔 및 필터링
   - 2) 민감 정보 마스킹 처리
   - 3) 코드 노이즈 제거
   - 4) 예산 할당 및 우선순위 결정
   - 5) 출력 파일 생성 및 저장 → **분리 권장**

#### TypeScript 파일
- 모든 TypeScript 함수는 50줄 미만

## 3. 설정 및 환경

### 설정 파일 목록
- `astro.config.mjs` - Astro 빌드 설정
- `package.json` - Node.js 의존성
- `tsconfig.json` - TypeScript 설정
- `.vscode/extensions.json` - VSCode 확장 추천
- `.vscode/launch.json` - VSCode 디버그 설정
- `wrangler.jsonc` - Cloudflare Workers 설정
- `.astro/settings.json` - Astro 내부 설정
- `.astro/data-store.json` - Astro 데이터 저장소
- `pipeline/config/buildings.json` - 빌딩 데이터 설정
- `scripts/data/heritage_list.json` - 문화재 목록 데이터
- `scripts/data/progress.json` - 진행 상태 데이터

### 환경변수 사용 현황
- 프로젝트 내에서는 `os.environ`, `os.getenv`, `process.env` 직접 사용 없음
- 외부 의존성(`node_modules`)에서만 사용됨

### 하드코딩된 경로나 URL
**하드코딩된 URL 목록 (src/lib/api.ts:23-81, src/lib/landmarks.ts:23-185):**
- `https://www.heritage.go.kr/gung/gogung1/images/ic-c1.jpg`
- `https://www.heritage.go.kr/gung/gogung2/images/img_changdeok_story_bg_00_00.jpg`
- `https://www.heritage.go.kr/heri/gungDetail/gogungListOpenApi.do`
- `https://www.heritage.go.kr/heri/gungDetail/gogungDetailOpenApi.do`
- `http://www.khs.go.kr/unisearch/images/national_treasure/2685609.jpg`

**총 하드코딩 URL: 21개** (모두 한국 문화재 공식 사이트)

## 4. 의존성 및 테스트

### 의존성 (`package.json`)
```json
{
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.12",
    "astro": "^5.17.1"
  }
}
```

**주요 의존성:**
- `astro`: 정적 사이트 생성기
- `@astrojs/cloudflare`: Cloudflare Workers 배포 통합

### 테스트 환경
- `tests/` 디렉토리 없음
- `*.test.*`, `*.spec.*` 파일 없음
- 테스트 프레임워크 설정 없음

### CI/CD 설정
- `.github/workflows` 디렉토리 없음
- `Makefile` 없음
- `Dockerfile` 없음
- `docker-compose.yml` 없음
- 배포: Cloudflare Workers (`wrangler.jsonc`)

## 5. 문서화

### README.md 내용 요약
- Astro Minimal 템플릿 기본 README
- 프로젝트 구조 및 명령어 설명
- 실제 프로젝트 내용 반영되지 않음 (템플릿 그대로)

### CLAUDE.md / AGENTS.md
- 존재하지 않음

### 주요 함수/클래스의 docstring 비율
- **Python 파일**: docstring 있음 (함수별 설명)
- **TypeScript 파일**: docstring 0개 (전체 함수 12개 중 0개)
- **docstring 비율**: 약 8.3% (Python만 고려시)

### 인터페이스/타입 문서화
- TypeScript 인터페이스 정의는 있으나 설명 주석 없음
- API 응답 구조는 인터페이스로 정의됨

---

## 종합 평가

### 강점
1. **명확한 프로젝트 구조**: 파이프라인, 소스, 빌드 디렉토리 분리
2. **타입 안정성**: TypeScript 사용으로 인터페이스 정의 완전
3. **최소 의존성**: 핵심 기능만 의존성으로 유지

### 개선점
1. **긴 함수 리팩토링 필요**: `export_project` 함수(146줄)는 여러 책임을 가짐
2. **테스트 부재**: 테스트 파일 및 CI/CD 파이프라인 없음
3. **문서화 부족**: TypeScript 함수에 docstring 전무
4. **하드코딩 URL**: 환경변수나 설정 파일로 이동 필요
5. **README 업데이트 필요**: 실제 프로젝트 내용으로 갱신 필요

### 권장 사항
1. `export_project` 함수를 파일 처리, 마스킹, 출력 생성 등으로 분리
2. 테스트 프레임워크 도입 (Vitest/Jest)
3. 주요 함수에 JSDoc 주석 추가
4. URL을 환경변수 또는 설정 파일로 이동
5. 프로젝트별 README 작성